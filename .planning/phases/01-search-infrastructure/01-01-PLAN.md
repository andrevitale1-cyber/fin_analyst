# Phase 1: Search Infrastructure

Implement the core search capability to allow `AutoFetcher` to find links via external search APIs.

## User Review Required

> [!IMPORTANT]
> **API Key**: You will need to provide a `TAVILY_API_KEY` in your `.env.local` file for this to work. Tavily has a generous free tier for developers.

## Proposed Changes

### [Component] Backend Agents [MODIFY]

#### [NEW] [search_client.py](file:///c:/Users/Andr%C3%A9%20Vitale/OneDrive/projeto_analise1/backend/agents/search_client.py)
Create a dedicated client for search operations.
- Implementation using Tavily API.
- Method `search_financial_reports(ticker, year, quarter)` returns a list of candidate URLs.

#### [MODIFY] [auto_fetcher.py](file:///c:/Users/Andr%C3%A9%20Vitale/OneDrive/projeto_analise1/backend/agents/auto_fetcher.py)
- Import `SearchClient`.
- Add a new method `fetch_via_search(ticker, ano, trimestre)` that uses the new search client.
- Update `fetch_result_pdf` to try search if heuristics fail.

## Verification Plan

### Automated Tests
- Create `backend/tests/test_search.py` to verify Tavily integration (mocked and live).

### Manual Verification
- Run a search for a less common ticker and verify it finds a PDF link in the output.
